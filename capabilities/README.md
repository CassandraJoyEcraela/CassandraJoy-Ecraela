# Capabilities

One folder per capability, named for the skill itself — never for a course, a
term, or a week. Each capability folder holds a README (what it is), a spec
(how I run it), and the model or workbook behind it.

Current:
- `marginal-analysis/`
